# profiles/shield-tablet.conf — SSH profil pro Termux na nvidia SHIELD tablet
# Pozn.: SSH server v Termuxu na tomto zařízení běží na portu 8022,
# proto je port v tomto profilu odlišný od výchozích 22.

name=nvidia SHIELD tablet (Termux)
host=192.168.1.12
user=user
port=8022
identity=~/.ssh/server
